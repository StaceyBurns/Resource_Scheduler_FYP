import { ScheduleDatePipe } from './schedule-date.pipe';

describe('ScheduleDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ScheduleDatePipe();
    expect(pipe).toBeTruthy();
  });
});
